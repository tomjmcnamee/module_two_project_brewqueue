<h1>All Visits</h1>

<ul>
    <% @visits.each do |visit|%>
    <li> <%= link_to visit.event_name, visit_path(visit.id)%></li>
    <%end%>
</ul>

<div><%= button_to "Add Visit", new_visit_path, method: :get%></div>
